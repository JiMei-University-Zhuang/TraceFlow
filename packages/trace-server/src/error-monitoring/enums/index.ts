export enum ErrorSeverity {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}

export enum ErrorCategory {
  RUNTIME = 'runtime',
  SYNTAX = 'syntax',
  NETWORK = 'network',
  SECURITY = 'security',
  CUSTOM = 'custom',
  PERFORMANCE = 'performance',
  RESOURCE = 'resource',
  PROMISE = 'promise',
}
